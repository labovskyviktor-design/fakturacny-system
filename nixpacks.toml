# Railway Nixpacks konfigurácia
[phases.setup]
nixPkgs = ["python311", "gcc", "postgresql", "pango", "cairo", "gdk-pixbuf", "libwebp", "fontconfig", "libffi", "glib", "shared-mime-info", "libxml2", "libxslt", "harfbuzz", "fribidi"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 120 --log-level debug --capture-output"
